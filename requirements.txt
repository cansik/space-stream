# general imports
configargparse
onnxruntime; platform_system != "Darwin" and platform.machine != 'arm64'
visiongraph[numba]; platform_system != "Darwin" and platform.machine != 'arm64'
visiongraph[realsense, azure, mediapipe, openvino]>=0.1.21
pyopengl

# gui related
open3d
duit[all]==0.1.0

# macos
syphonpy; platform_system == "Darwin"
glfw; platform_system == "Darwin"

# windows
SpoutGL; platform_system == "Windows"