# general imports
configargparse
pyopengl

# visiongraph
onnxruntime; platform_system != "Darwin" and platform.machine != "arm64"
visiongraph[numba]>=0.1.23.6; platform_system != "Darwin" and platform.machine != "arm64"
visiongraph[realsense, azure, mediapipe, openvino]>=0.1.23.6

# gui related
open3d
duit[all]>=0.1.2

# macos
syphonpy; platform_system == "Darwin"
glfw; platform_system == "Darwin"

# windows
SpoutGL; platform_system == "Windows"