--find-links https://github.com/cansik/data-storage/releases/tag/packages

# general imports
configargparse
visiongraph[realsense, azure, mediapipe, openvino, onnx, numba]>=0.1.17
pyopengl

# gui related
open3d
simbi[all]==0.1.0

# macos
syphonpy; platform_system == "Darwin"
glfw; platform_system == "Darwin"

# windows
SpoutGL; platform_system == "Windows"